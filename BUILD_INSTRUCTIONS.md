# BUILD INSTRUCTIONS: home-maintenance-household-records-manager

## Overview

- **App Name:** home-maintenance-household-records-manager
- **Repository:** home-maintenance-household-records-manager
- **Tech Stack:** Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui
- **Description:** A household operations app for homeowners, landlords, and families to manage recurring maintenance tasks alongside important home and family reference information. It combines maintenance calendars, issue notes, appliance and insurance records, and other practical household data in one place.

---

## Tech Stack & Setup

### Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui

```bash
npx create-next-app@latest home-maintenance-household-records-manager --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
cd home-maintenance-household-records-manager
# shadcn/ui (always include)
npx shadcn@latest init -d
npx shadcn@latest add button card input label form toast badge
# Only add Prisma if the PRD requires data persistence:
# npm install prisma @prisma/client
# npx prisma init
# Only add NextAuth if the PRD requires user login:
# npm install next-auth@beta @auth/prisma-adapter bcryptjs
# npm install -D @types/bcryptjs
# Only add Stripe if the PRD explicitly requires payments:
# npm install stripe @stripe/stripe-js
# Only add Resend if the PRD explicitly requires email:
# npm install resend
```

### Configuration

Add to next.config.ts:
```ts
const nextConfig = {
  output: "standalone",
};
export default nextConfig;
```

### Development

```bash
npm run dev
```

### Build

```bash
npm run build
```

---

## Product Requirements Document

The full PRD is included below. Implement ALL features described.

---

# Home Maintenance and Household Records Manager

*Generated by Forge on 2026-04-17T00:58:36.317Z — score 8.2/10*

## Overview
Home Maintenance and Household Records Manager is a web app for homeowners, first-time buyers, and small landlords who need one simple place to manage recurring home upkeep and important household records. The core problem is that people currently juggle Apple Notes, spreadsheets, email receipts, paper manuals, and generic to-do apps that are either too flexible to be easy or too bloated to feel practical. This product solves that by combining a maintenance calendar, guided recurring tasks, appliance and home asset records, document storage, and household issue logging into a lightweight “home operations” app built specifically for real home ownership admin.

## Target user
Primary persona: a first-time homeowner or organized family household manager, age 28–45, who owns 1 home and wants to stay on top of maintenance without building their own system.

What they currently do:
- Keep appliance photos, receipts, and manuals in Apple Notes, Google Drive, email, or a kitchen drawer
- Use calendar reminders or generic to-do apps for things like HVAC filter changes and gutter cleaning
- Forget service dates, warranty expirations, and contractor details
- Struggle to get a spouse/partner to use Notion, Airtable, or spreadsheets

Why current options fail them:
- Generic task apps do not understand home-specific recurring maintenance
- Home inventory apps feel heavy, dated, or upsell aggressively
- DIY systems take too long to set up and are not spouse-friendly
- Most tools are either “catalog everything” or “manage chores,” not “run your home”
- They want practical structure: what to do, when to do it, and where to find the related records

Secondary persona: a small landlord managing 1–5 rental units who wants a lightweight record system for appliance info, recurring tasks, and issue history without using enterprise property software.

## Core features
- Household and property setup
  - Create one or more properties
  - Store address, property type, move-in date, square footage, and notes
  - Choose a default maintenance template based on property type

- Guided recurring maintenance tasks
  - Preloaded task templates such as HVAC filter replacement, smoke detector battery check, water heater flush, gutter cleaning, dryer vent cleaning
  - Each task has frequency, season/month, description, and why it matters
  - User can enable/disable suggested tasks per property

- Maintenance task dashboard
  - Show upcoming, overdue, and completed tasks
  - Mark task complete
  - Auto-generate next due date based on recurrence rule
  - Filter by property and by status

- Appliance and home asset records
  - Add appliances/assets with name, category, brand, model, serial number, purchase date, install date, warranty end date, location in home, notes
  - Attach one or more files/photos per record
  - Link maintenance tasks to an appliance where relevant

- Document vault
  - Upload and store PDFs/images for receipts, manuals, warranties, insurance docs, permits, service invoices
  - Tag each document by type and optionally link it to a property or appliance
  - Search by title/type

- Household issue log
  - Create issue notes with title, description, room/area, date noticed, status, and optional photos
  - Mark status as open, monitoring, resolved
  - Useful for leaks, cracks, appliance problems, recurring concerns

- Shared household access
  - Invite one other household member by email
  - Both users can view and edit the same property data
  - Keep permissions simple for MVP: owner and member

- Email reminders
  - Daily cron checks for tasks due in 7 days and overdue tasks
  - Send reminder emails with task name, property, due date, and direct link
  - Weekly digest option only; no push notifications in MVP

- Search and quick retrieval
  - Global search across tasks, appliances, documents, and issues
  - Fast “find my furnace model/manual/warranty” use case

- Basic onboarding
  - First-run wizard: create property, choose template, add first appliance, enable reminders
  - Seed useful default tasks immediately so app feels valuable on day 1

## Tech stack recommendation
Use the standard web stack. This product does not require native mobile capabilities for MVP. The strongest differentiation is not phone hardware access; it is structured homeowner workflows and easy household sharing. A responsive web app ships faster, avoids App Store overhead, supports SEO for high-intent homeowner searches, and is sufficient for document upload, reminders, and day-to-day use.

Recommended stack:
- Next.js 15.x (App Router) + TypeScript 5.x
- PostgreSQL 16 + Prisma ORM 6.x
- Tailwind CSS 3.x + shadcn/ui
- NextAuth v5 with Google OAuth + email magic link optional later
- Stripe for billing
- Resend for transactional emails and reminders
- Coolify for deployment using Docker
- Supabase Storage or S3-compatible object storage for file uploads
- React Hook Form + Zod for forms/validation
- TanStack Table for record tables where useful
- date-fns for recurrence/date handling
- UploadThing or direct S3/Supabase signed upload flow for documents/photos
- Lucide React for icons

Specific package recommendations:
- `next@15`
- `react@19`
- `typescript@5`
- `prisma@6`
- `@prisma/client@6`
- `next-auth@5`
- `@auth/prisma-adapter`
- `stripe`
- `resend`
- `zod`
- `react-hook-form`
- `@hookform/resolvers`
- `date-fns`
- `zustand` for light client state if needed
- `@tanstack/react-table`
- `sonner` for toasts

Why this stack:
- Fastest path for a solo builder
- Strong fit for CRUD-heavy app with auth, billing, email, and file storage
- SEO-friendly for acquisition around homeowner keywords
- Responsive web app is enough for “check records and complete tasks” behavior
- Can later wrap in a mobile shell or build native apps if traction warrants it

## Monetization
Primary strategy: freemium with one paid tier, billed annually-first in positioning.

Why:
- The “infrequency problem” makes a pure subscription pitch weaker unless the free tier proves value first
- Users in research explicitly mention willingness to pay if the product actually works, and some are price-sensitive
- A free tier helps SEO and referral growth
- A single paid tier keeps implementation and messaging simple

Pricing:
- Free
  - 1 property
  - Up to 10 appliances/assets
  - Up to 20 documents
  - Core maintenance reminders
  - 1 household member total
- Home Pro — $49/year or $6.99/month
  - Up to 3 properties
  - Unlimited appliances/assets
  - Unlimited documents
  - Unlimited issue logs
  - Shared household access
  - Advanced reminder emails
  - Exportable home summary packet for resale/insurance
  - Priority support

Recommendation:
- Default checkout emphasis on annual plan at $49/year
- Monthly exists for flexibility but should be visually de-emphasized
- No multiple premium tiers in MVP

## Keyword strategy
Primary SEO keywords:
- home maintenance app
- home maintenance checklist app
- household records organizer
- appliance warranty tracker app
- home information vault
- family records manager
- home upkeep reminders
- household data app

Long-tail targets:
- home maintenance app with reminders
- appliance warranty tracker app
- home appliance serial number tracker
- household records organizer app
- home records for house sale
- centriq alternative
- home maintenance checklist for first-time homeowners
- where to store appliance manuals and receipts
- home maintenance schedule by season

Suggested meta title format:
- {Primary keyword} | {Brand}
- Example: Home Maintenance App with Reminders | HearthLog

Suggested meta description format:
- Keep up with home maintenance, appliance records, warranties, manuals, and household documents in one simple app. Get reminders, track service history, and stay organized.

Pages to optimize:
- Homepage: “home maintenance app”
- /home-maintenance-checklist: seasonal checklist landing page
- /appliance-warranty-tracker: appliance records use case page
- /household-records-organizer: document vault use case page
- /centriq-alternative: competitor comparison page
- /for-first-time-homeowners: persona page
- /for-small-landlords: secondary persona page
- Blog posts:
  - Home maintenance checklist by month
  - What records to keep after buying a house
  - How to organize appliance manuals and warranties
  - What to hand over when selling a house

## Revenue model
Pricing strategy:
- Free tier for acquisition and activation
- One paid tier: Home Pro at $49/year or $6.99/month
- Assume 75% of paid users choose annual, 25% choose monthly
- Effective blended monthly revenue per paid user:
  - Annual: $49/year = $4.08/month
  - Monthly: $6.99/month
  - Blended ARPU ≈ $4.81/month per paid user

Assumptions:
- Free-to-paid conversion starts low and improves with onboarding and reminder quality
- SEO and community acquisition drive most early traffic
- Paid conversion stronger among users who add at least 1 property + 3 appliances + 1 document
- Month 12 target is still a lean bootstrapped business, not venture-scale

Projected users and revenue:

Month 1
- 300 total signups
- 180 activated users
- 9 paid users
- Free-to-paid conversion: 3%
- MRR equivalent: 9 × $4.81 = $43
- Cash collected if mostly annual: roughly $330–$380

Month 3
- 1,500 total signups
- 850 activated users
- 53 paid users
- Free-to-paid conversion: 3.5%
- MRR equivalent: 53 × $4.81 = $255
- Cash collected annual-heavy: roughly $2,000–$2,400 cumulative

Month 6
- 5,000 total signups
- 2,800 activated users
- 200 paid users
- Free-to-paid conversion: 4%
- MRR equivalent: 200 × $4.81 = $962
- Cash collected annual-heavy: roughly $8,000–$9,500 cumulative

Month 12
- 18,000 total signups
- 9,500 activated users
- 630 paid users
- Free-to-paid conversion: 3.5% overall or 6–8% among activated core users
- MRR equivalent: 630 × $4.81 = $3,030
- Annualized run rate: ~$36,000

Upside case aligned to provided market data:
- 2,000–6,000 MRR is realistic if SEO pages rank and annual conversion improves
- To hit $2,000 MRR at blended ARPU of $4.81, need ~416 paid users
- To hit $6,000 MRR, need ~1,247 paid users
- This is plausible within 12–24 months if acquisition compounds through search and comparison pages

Key funnel targets:
- Visitor to signup: 4–8%
- Signup to activated: 55–65%
- Activated to paid: 6–10%
- Overall free-to-paid: 3–5%

## Marketing plan
Launch strategy for week 1:
- Launch with a clear positioning statement: “The app for running your home: maintenance reminders, appliance records, and household documents in one place.”
- Do not market as a generic family organizer
- Create 3 SEO landing pages and 2 comparison pages before public launch
- Seed with screenshots showing recurring maintenance + appliance record + document attachment in one flow

SEO/content plan:
Create these pages first:
- Homepage targeting “home maintenance app”
- /home-maintenance-checklist-app
- /appliance-warranty-tracker-app
- /household-records-organizer
- /centriq-alternative
- /homezada-alternative
- /for-first-time-homeowners

First 10 blog posts:
- Annual home maintenance checklist for first-time homeowners
- Monthly home maintenance schedule by season
- Best way to organize appliance manuals and warranties
- What household records to keep after buying a house
- How to track appliance serial numbers and receipts
- What to include in a home resale records packet
- Centriq alternative for appliance and home records
- HomeZada alternative for simpler home maintenance tracking
- How to create a home maintenance calendar
- Best apps for home maintenance reminders

Community seeding:
Specific communities from research:
- r/homeowners
- r/FirstTimeHomeBuyer
- r/RealEstate
- r/personalfinance

Approach:
- Do not spam launch links
- Post useful templates/checklists first
- Offer a free downloadable seasonal maintenance checklist with optional signup
- Ask for beta testers from “I built this because I was using Notes/spreadsheets” angle
- Share comparison posts only where allowed

Other communities:
- Facebook groups for first-time homeowners
- BiggerPockets forums for small landlords
- Product Hunt launch in week 5
- Indie Hackers build-in-public thread
- Hacker News “Show HN” only if product is polished and fast

Social media:
- Pinterest: seasonal maintenance checklist graphics
- TikTok/Instagram Reels: quick “3 home records every homeowner should save” clips
- X/LinkedIn: founder-led posts around homeowner admin pain and before/after organization
- Focus less on brand personality, more on concrete homeowner jobs

Paid acquisition:
Month 1–3 budget: $3,000–$6,000 initially
Allocation:
- Google Search Ads: 40%
  - Target high-intent terms like “home maintenance app,” “appliance warranty tracker,” “centriq alternative”
- Meta Ads: 35%
  - Target first-time homeowners, recent movers, home organization interests
- Pinterest Ads: 15%
  - Seasonal checklist and home organization creatives
- Retargeting: 10%

Expected CAC:
- $8–$25 from provided estimate
- Paid user payback:
  - Annual plan payback can happen on first purchase
  - Monthly plan payback within 1–2 months if CAC is below $14

Expected 3-month ROI:
- With $4,500 spend and blended CAC of $15, acquire ~300 paid users in optimistic paid-search-heavy case
- More conservative: 120–180 paid users
- At 150 paid users and blended ARPU $4.81 MRR equivalent, immediate MRR is ~$721, but annual cash flow is healthier
- Paid should be used to validate messaging, not as the only growth engine

## Competitor landscape
### HomeZada
- Pricing model:
  - Subscription-oriented homeowner platform
- Key features:
  - Home inventory
  - Maintenance scheduling
  - Financial/home value features
  - Document storage
- Weaknesses:
  - Cluttered and dated UI
  - Onboarding friction
  - Feels like a full home management suite rather than a lightweight household ops tool
  - Upsell-heavy perception
- Our differentiation:
  - Simpler setup in under 5 minutes
  - Cleaner mobile-first responsive UX
  - Focused on maintain + store records, not finance sprawl
  - One clear paid tier

### Centriq
- Pricing model:
  - Consumer app with premium features; historically strong appliance/home inventory positioning
- Key features:
  - Appliance records
  - Manuals
  - Maintenance reminders
  - Home inventory elements
- Weaknesses:
  - Reduced product investment post-acquisition
  - Users actively looking for alternatives
  - More inventory-centric than recurring household operations
- Our differentiation:
  - Position directly as a Centriq alternative
  - Better recurring maintenance dashboard
  - Stronger issue log and household sharing
  - Active development and modern UX

### BrightNest
- Pricing model:
  - Content/tips-oriented
- Key features:
  - Home tips and maintenance guidance
- Weaknesses:
  - Lacks structured personal records
  - No warranty/manual/document vault
  - Not a true operational system
- Our differentiation:
  - Combines guidance with actual storage and reminders
  - Personalized to the user’s property and assets

### Sortly / generic inventory apps
- Pricing model:
  - Subscription inventory software
- Key features:
  - Cataloging items
  - Photos
  - Labels/tags
- Weaknesses:
  - Inventory-first, not homeowner workflow-first
  - Weak recurring maintenance support
  - Poor fit for seasonal home upkeep
- Our differentiation:
  - Built around maintenance schedule and home admin
  - Appliance and document records exist to support upkeep, not generic cataloging

### Notion / spreadsheets / Apple Notes
- Pricing model:
  - Free or already-owned tools
- Key features:
  - Flexible notes and tables
- Weaknesses:
  - High setup friction
  - No built-in recurrence logic
  - No spouse-friendly structure
  - No purpose-built home templates
- Our differentiation:
  - Zero-setup onboarding
  - Home-specific templates
  - Shared household access out of the box
  - Searchable records and reminders without DIY work

## Timeline
### Week 1–2: core MVP
Week 1
- Set up project, auth, database, file storage, billing scaffold
- Build data models for users, households, properties, tasks, appliances, documents, issues, memberships
- Implement onboarding wizard
- Build property creation flow
- Seed default maintenance templates

Week 2
- Build maintenance dashboard
- Add complete-task flow with next due date generation
- Build appliance CRUD
- Build document upload and attachment to property/appliance
- Build issue log CRUD

### Week 3: polish + testing
- Add household sharing/invite flow
- Add global search
- Add reminder email jobs
- Improve dashboard states, empty states, and mobile responsiveness
- Add analytics events for activation funnel
- Write seed data and test accounts
- Add error handling and permissions checks

### Week 4: beta + marketing prep
- Invite 20–30 beta users from homeowner communities and personal network
- Fix onboarding friction and confusing labels
- Add exportable home summary packet as a simple PDF/HTML print view
- Build homepage, pricing page, and key SEO landing pages
- Set up Stripe checkout and customer portal
- Prepare launch assets: screenshots, explainer GIF, comparison pages

### Week 5: launch + distribution
- Launch on Product Hunt
- Publish 3 blog posts and 2 comparison pages
- Post checklist lead magnet to homeowner communities where allowed
- Run small-budget search ads for “centriq alternative” and “home maintenance app”
- Monitor signup funnel, activation, and payment conversion daily
- Ship first post-launch fixes within 72 hours

## Score breakdown
- recurringRevenue: 7/10 — Strong utility and annual-plan fit, but usage frequency makes pure subscription stickiness weaker than daily-use apps.
- buildComplexity: 6/10 — Mostly straightforward CRUD, reminders, sharing, and file storage; manageable for a solo builder.
- marketValidation: 8/10 — Strong evidence from search intent and repeated homeowner complaints about existing solutions.
- competition: 4/10 — Real competition exists from HomeZada, Centriq, and DIY tools, so positioning must be sharper than “household organizer.”
- automationPotential: 8/10 — Templates, recurrence, reminders, and guided setup create clear product leverage.
- platformFit: 9/10 — Web app fits the workflow well and supports SEO, sharing, and document management without native complexity.

## Build instructions for Codex
1. Initialize project:
   - Run `npx create-next-app@latest home-maintenance-household-records-manager --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`
   - Update app name references to a working brand like `HearthLog`

2. Install dependencies:
   - Run `npm install prisma @prisma/client next-auth @auth/prisma-adapter stripe resend zod react-hook-form @hookform/resolvers date-fns @tanstack/react-table sonner lucide-react`
   - Install any storage SDK chosen, preferably Supabase or S3-compatible client

3. Install and configure shadcn/ui:
   - Run `npx shadcn@latest init`
   - Add components: button, card, input, label, form, textarea, dialog, dropdown-menu, sheet, table, badge, tabs, calendar, select, toast, alert-dialog, skeleton

4. Set up Prisma:
   - Run `npx prisma init`
   - Configure PostgreSQL connection in `.env`
   - Define schema in `prisma/schema.prisma` with these models:
     - `User`
     - `Account`
     - `Session`
     - `VerificationToken`
     - `Household`
     - `HouseholdMember`
     - `Property`
     - `MaintenanceTemplate`
     - `MaintenanceTask`
     - `Appliance`
     - `Document`
     - `Issue`
     - `Invitation`
     - `Subscription`
   - Run `npx prisma generate` and `npx prisma migrate dev`

5. Implement Prisma schema details:
   - `Household`: id, name, ownerId, createdAt
   - `HouseholdMember`: id, householdId, userId, role (`OWNER` or `MEMBER`)
   - `Property`: id, householdId, name, address1, city, state, postalCode, country, propertyType, moveInDate, squareFeet, notes
   - `MaintenanceTemplate`: id, propertyType, title, description, frequencyUnit, frequencyInterval, seasonalMonth, applianceCategory nullable
   - `MaintenanceTask`: id, propertyId, applianceId nullable, title, description, whyItMatters, status, dueDate, lastCompletedAt nullable, frequencyUnit, frequencyInterval, seasonalMonth nullable, isSuggested, createdAt
   - `Appliance`: id, propertyId, name, category, brand, model, serialNumber, purchaseDate nullable, installDate nullable, warrantyEndDate nullable, location, notes
   - `Document`: id, propertyId nullable, applianceId nullable, issueId nullable, title, fileUrl, fileType, docType, uploadedById
   - `Issue`: id, propertyId, title, description, area, status, dateNoticed, resolvedAt nullable
   - `Invitation`: id, householdId, email, role, token, expiresAt, acceptedAt nullable
   - `Subscription`: id, userId, stripeCustomerId, stripeSubscriptionId nullable, stripePriceId nullable, status, currentPeriodEnd nullable

6. Configure NextAuth:
   - Create `src/auth.ts`
   - Use Google OAuth and Prisma adapter
   - Add session callback to include `user.id`
   - Create auth route in `src/app/api/auth/[...nextauth]/route.ts`
   - Add middleware to protect app routes under `/app`

7. Build app shell and route structure:
   - Public routes:
     - `/`
     - `/pricing`
     - `/login`
     - `/blog/[slug]`
     - `/centriq-alternative`
     - `/homezada-alternative`
   - Authenticated routes:
     - `/app`
     - `/app/onboarding`
     - `/app/tasks`
     - `/app/appliances`
     - `/app/documents`
     - `/app/issues`
     - `/app/settings`
   - Create shared layout in `src/app/app/layout.tsx` with sidebar nav

8. Seed maintenance templates:
   - Create `prisma/seed.ts`
   - Add 20–30 default maintenance templates by property type:
     - HVAC filter replacement
     - Smoke detector battery check
     - Water heater flush
     - Gutter cleaning
     - Dryer vent cleaning
     - Refrigerator coil cleaning
     - Sump pump test
     - Fireplace inspection
   - Include `whyItMatters`, `frequencyUnit`, `frequencyInterval`, and optional `seasonalMonth`

9. Build onboarding wizard:
   - Files:
     - `src/app/app/onboarding/page.tsx`
     - `src/components/onboarding/property-form.tsx`
     - `src/components/onboarding/template-selector.tsx`
     - `src/components/onboarding/first-appliance-form.tsx`
   - Steps:
     - Create household
     - Create first property
     - Select property type
     - Seed suggested tasks from templates
     - Add first appliance
     - Enable reminder emails
   - Redirect completed users to `/app`

10. Build maintenance dashboard:
   - Create `src/app/app/page.tsx`
   - Sections:
     - overdue tasks
     - due soon
     - completed recently
   - Add task actions:
     - mark complete
     - edit due date
     - skip once
   - Implement recurrence helper in `src/lib/recurrence.ts` using `date-fns`

11. Build task management pages:
   - `src/app/app/tasks/page.tsx`
   - `src/components/tasks/task-table.tsx`
   - `src/components/tasks/task-form.tsx`
   - Support filters by property, status, month
   - On complete, set `lastCompletedAt` and compute next `dueDate`

12. Build appliance records:
   - `src/app/app/appliances/page.tsx`
   - `src/components/appliances/appliance-form.tsx`
   - `src/components/appliances/appliance-table.tsx`
   - Support fields for brand/model/serial/warranty/location
   - Add linked documents section on each appliance detail drawer or modal

13. Build document vault:
   - `src/app/app/documents/page.tsx`
   - `src/components/documents/document-upload.tsx`
   - `src/components/documents/document-table.tsx`
   - Implement upload flow to object storage
   - Store metadata in `Document` table
   - Allow linking document to property, appliance, or issue
   - Add document type enum: `MANUAL`, `RECEIPT`, `WARRANTY`, `INSURANCE`, `PERMIT`, `INVOICE`, `OTHER`

14. Build issue log:
   - `src/app/app/issues/page.tsx`
   - `src/components/issues/issue-form.tsx`
   - `src/components/issues/issue-list.tsx`
   - Allow status values: `OPEN`, `MONITORING`, `RESOLVED`
   - Support optional photo/document attachments

15. Build household sharing:
   - `src/app/app/settings/page.tsx`
   - `src/components/settings/invite-member-form.tsx`
   - Create invitation token flow with Resend email
   - Accept invite route: `src/app/invite/[token]/page.tsx`
   - Restrict free tier to one household member total; enforce in server actions

16. Set up Stripe:
   - Create products/prices for Free, Home Pro monthly, Home Pro annual
   - Add checkout route and billing portal
   - Implement webhook at `src/app/api/webhooks/stripe/route.ts`
   - Update `Subscription` table on checkout success, renewal, cancellation
   - Gate premium limits in `src/lib/plan.ts`

17. Add reminder emails and cron job:
   - Create `src/lib/reminders.ts`
   - Query tasks due in 7 days and overdue
   - Send weekly digest emails via Resend
   - Expose secure cron route `src/app/api/cron/reminders/route.ts`
   - Trigger from Coolify cron or external scheduler once daily

18. Add global search and plan limits:
   - Create `src/components/search/global-search.tsx`
   - Implement simple server-side search across tasks, appliances, documents, issues
   - Enforce free plan limits:
     - 1 property
     - 10 appliances
     - 20 documents
   - Show upgrade prompts only when user hits a limit, not constantly

19. Build marketing pages and SEO:
   - Create homepage in `src/app/page.tsx`
   - Create pricing page in `src/app/pricing/page.tsx`
   - Create comparison pages and use-case pages
   - Add metadata exports for each page
   - Add FAQ schema and basic Open Graph tags
   - Include screenshots and clear CTA: “Start free”

20. Final verification and deployment:
   - Add role/household permission checks in all server actions and route handlers
   - Test signup, onboarding, task completion, uploads, invites, billing, and reminders
   - Ensure `npm run build` passes
   - Configure `next.config.ts` for `output: "standalone"`
   - Create `Dockerfile`
   - Deploy PostgreSQL and app via Coolify
   - Set environment variables for auth, Stripe, Resend, storage, and cron secret
   - Run production migration and seed script
   - Add basic analytics events for signup, onboarding completion, first appliance added, first document uploaded, and upgrade checkout started


---

## Dockerfile

```dockerfile
# ============================================================
# BASE DOCKERFILE — for apps WITHOUT a database
# If this app uses Prisma/SQLite, see the "With Prisma" section below
# ============================================================

# Stage 1: Dependencies
FROM node:20-slim AS deps
WORKDIR /app
COPY package.json package-lock.json* ./
# --ignore-scripts prevents postinstall hooks (e.g. prisma generate) from running
# here where schema.prisma isn't available yet. Scripts run in the builder stage.
RUN npm ci --ignore-scripts

# Stage 2: Build
FROM node:20-slim AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
# Provide build-time defaults so the build succeeds with no env vars configured
ENV AUTH_SECRET="build-time-placeholder-secret"
ENV NEXT_PUBLIC_APP_URL="https://localhost:3000"
# Run any postinstall scripts now that all source files are present
RUN npm rebuild 2>/dev/null || true
RUN npm run build

# Stage 3: Runner
FROM node:20-slim AS runner
WORKDIR /app
ENV NODE_ENV=production
# Default AUTH_SECRET — app works with no Coolify env vars; override for production
ENV AUTH_SECRET="forge-app-default-secret-override-in-production"
ENV NEXT_PUBLIC_APP_URL=""

RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

USER nextjs
EXPOSE 3000
ENV PORT=3000
CMD ["node", "server.js"]

# ============================================================
# WITH PRISMA/SQLITE — replace the above Dockerfile entirely
# Use this when the PRD requires data persistence or user auth
# ============================================================
#
# FROM node:20-slim AS deps
# WORKDIR /app
# COPY package.json package-lock.json* ./
# # --ignore-scripts prevents prisma generate from running before schema.prisma is copied
# RUN npm ci --ignore-scripts
#
# FROM node:20-slim AS builder
# # Install OpenSSL for Prisma schema engine (debian-openssl-3.0.x target)
# RUN apt-get update -y && apt-get install -y openssl && rm -rf /var/lib/apt/lists/*
# WORKDIR /app
# COPY --from=deps /app/node_modules ./node_modules
# COPY . .
# ENV DATABASE_URL="file:/tmp/build.db"
# ENV AUTH_SECRET="build-time-placeholder-secret"
# ENV NEXT_PUBLIC_APP_URL="https://localhost:3000"
# # Generate Prisma client now that schema.prisma is available
# RUN npx prisma generate
# RUN npm run build
#
# FROM node:20-slim AS runner
# # Install OpenSSL for Prisma schema engine at runtime
# RUN apt-get update -y && apt-get install -y openssl && rm -rf /var/lib/apt/lists/*
# WORKDIR /app
# ENV NODE_ENV=production
# ENV DATABASE_URL="file:/data/app.db"
# ENV AUTH_SECRET="forge-app-default-secret-override-in-production"
# ENV NEXT_PUBLIC_APP_URL=""
# RUN addgroup --system --gid 1001 nodejs
# RUN adduser --system --uid 1001 nextjs
# RUN mkdir -p /data && chown nextjs:nodejs /data
# COPY --from=builder /app/public ./public
# COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
# COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static
# COPY --from=builder --chown=nextjs:nodejs /app/prisma ./prisma
# # Copy full node_modules so the Prisma CLI has all its runtime deps (v6+ requires effect, c12, etc.)
# COPY --from=builder --chown=nextjs:nodejs /app/node_modules ./node_modules
# USER nextjs
# EXPOSE 3000
# ENV PORT=3000
# CMD ["sh", "-c", "node node_modules/prisma/build/index.js db push --skip-generate && echo 'DB schema initialized' && node server.js"]
```

---

## File Organization

- Keep source code in `src/` (if applicable)
- Components in `src/components/`
- Utilities in `src/lib/` or `src/utils/`
- API routes in `src/app/api/` (Next.js) or equivalent
- Static assets in `public/`
- Environment variables in `.env.local` (never commit secrets)

---

## Build Verification

Run `npm run build` and ensure zero errors before committing.

- No TypeScript errors
- No ESLint warnings (fix or disable with justification)
- All pages/screens render without crashes
- API routes return expected responses

---

## Git Instructions

When the build is complete and verified:

1. Initialize the repository if not already done: `git init`
2. Stage all files: `git add -A`
3. Commit: `git commit -m "Initial build: home-maintenance-household-records-manager"`
4. Push to remote when instructed

---

## Required Files Checklist

- [ ] `README.md` - Project description, setup instructions, tech stack
- [ ] `.gitignore` - Appropriate for Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui
- [ ] `.env.example` - Template for required environment variables (see below)
- [ ] `Dockerfile` - Production-ready container build
- [ ] `package.json` (or equivalent) - Dependencies and scripts

---

## Zero-Config Deployment Requirement

**CRITICAL: This app MUST start and run correctly with NO environment variables configured in the deployment platform (Coolify). All external service dependencies must either be absent or have safe fallback defaults baked in.**

Apps that crash on startup due to missing env vars will fail every deployment cycle.

**CRITICAL: Never instantiate third-party API clients at module level.** Next.js evaluates top-level code during `next build` — a `new Resend(...)`, `new Stripe(...)`, or `new OpenAI(...)` call at module scope will crash the build if the API key env var is missing. Always initialize these clients **inside** the handler/function body, guarded by an env var check.

---

## Environment Variables (.env.example)

Only include variables the app actually uses. If the PRD doesn't require a feature, don't add those variables.

```bash
# App URL (optional — remove if not needed)
NEXT_PUBLIC_APP_URL="https://yourdomain.com"

# --- Only include below if PRD requires data persistence ---
# Database (SQLite — no external service required)
DATABASE_URL="file:./dev.db"

# --- Only include below if PRD requires user login ---
# NextAuth secret — a default is baked into the Dockerfile; override here for production
AUTH_SECRET="your-secret-here"  # generate with: openssl rand -base64 32

# --- Only include below if PRD explicitly requires payments ---
# STRIPE_SECRET_KEY="sk_live_..."
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."

# --- Only include below if PRD explicitly requires email ---
# RESEND_API_KEY="re_..."
# EMAIL_FROM="noreply@yourdomain.com"
```

---

## Key Implementation Notes

### Database (only if PRD requires persistence)
- Use **SQLite** via Prisma: `provider = "sqlite"`, `url = env("DATABASE_URL")`
- `DATABASE_URL` defaults to `file:/data/app.db` in the Dockerfile — no Coolify config needed
- For local dev: `DATABASE_URL="file:./dev.db"` in `.env.local`
- Run `npx prisma db push` after schema changes
- **Do NOT use PostgreSQL** — no PostgreSQL server is provisioned in the deployment environment

### Authentication (only if PRD requires user login)
- Use **NextAuth v5** with the **Credentials provider** (email + password)
- **Do NOT use Google OAuth** — no OAuth credentials are configured in deployment
- Hash passwords with **bcrypt** (`bcryptjs` package)
- Session strategy: **`"jwt"`** (not `"database"`) — required for Credentials provider
- `AUTH_SECRET` defaults are baked into the Dockerfile; app works out of the box

### Payments (only if PRD explicitly requires it)
- Use Stripe only when the PRD's core functionality requires payment processing
- If adding Stripe: handle `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted` at `/api/webhooks/stripe`
- Make the app functional without Stripe credentials (show upgrade prompts rather than crashing)

### Email (only if PRD explicitly requires it)
- Use Resend only when the PRD requires transactional email
- **CRITICAL: NEVER instantiate `new Resend(...)` at module level** — always inside the handler function body with a guard:
  ```ts
  // WRONG — causes build failure (next build evaluates module-level code):
  const resend = new Resend(process.env.RESEND_API_KEY);
  export async function POST(req) { ... }

  // CORRECT — lazy init inside handler:
  export async function POST(req) {
    if (!process.env.RESEND_API_KEY) {
      console.log("[email] RESEND_API_KEY not set, skipping email");
      return NextResponse.json({ ok: true });
    }
    const { Resend } = await import("resend");
    const resend = new Resend(process.env.RESEND_API_KEY);
    ...
  }
  ```
- Next.js evaluates module-level code during `next build` page data collection — missing env vars at module scope crash the build
- Same rule applies to Stripe, OpenAI, and ALL third-party SDK clients — always lazy-initialize inside handler functions

### Deployment
- Coolify with Docker — always set `output: "standalone"` in next.config.ts
- Do NOT add `COPY public ./public` in the Dockerfile if the public/ directory is empty or doesn't exist
- The Dockerfile's CMD initializes the SQLite database on first start via `prisma db push`
- App must respond healthy on port 3000 within 60 seconds of container start
- **CRITICAL Dockerfile rule**: In the `deps` stage, always use `RUN npm ci --ignore-scripts`. Without `--ignore-scripts`, the `postinstall` hook (e.g. `prisma generate`) runs before `prisma/schema.prisma` is copied, causing the build to fail. Run `npx prisma generate` explicitly in the `builder` stage after `COPY . .`.
- **Do NOT run `npx prisma db push` in the Dockerfile builder stage** — it is not needed at build time. The CMD already runs `prisma db push` at container startup when a real `/data/app.db` path is available.
- **Use `node:20-slim` (not `node:20-alpine`)** — Debian slim avoids Alpine musl/libssl issues. Add `RUN apt-get update -y && apt-get install -y openssl && rm -rf /var/lib/apt/lists/*` to BOTH the builder and runner stages for Prisma's schema engine.
- **Set `binaryTargets` in schema.prisma** — always include `binaryTargets = ["native", "debian-openssl-3.0.x"]` in the Prisma generator block so Prisma generates the correct engine binary for the Debian container environment.
- **Copy full node_modules in the runner stage** — do NOT selectively copy `node_modules/.prisma`, `node_modules/prisma`, `node_modules/@prisma` separately. Instead use `COPY --from=builder --chown=nextjs:nodejs /app/node_modules ./node_modules` to copy everything. Prisma v6+ CLI has deep transitive deps (`@prisma/config` → `effect`, `c12`, etc.) that break if not all present.

